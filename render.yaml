services:
  - type: web
    name: ai-server
    env: python
    plan: free

    buildCommand: |
      pip install -r requirements.txt
      apt-get update && apt-get install -y ffmpeg

    startCommand: gunicorn app:app

    envVars:
      - key: PYTHON_VERSION
        value: 3.10.13